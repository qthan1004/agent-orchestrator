# Task EV06: scan_workspace MCP Tool

## Info
- **ID:** EV06-scan-workspace-tool
- **Module:** src/mcp-server/tools/
- **Group:** 2 (Workspace Memory Injection)
- **Dependencies:** EV01
- **Priority:** 6
- **Ref:** `dev-docs/plan_evolution-and-local-brain.md` — Phase 2, §2.1

## What to do

Tạo MCP tool `scan_workspace` — quét workspace và sinh `.agent/workspace-memory.md`.

### [NEW] `src/mcp-server/tools/scan-workspace.mjs`

> Hoặc `.ts` nếu migration M01-M06 đã xong.

**Logic:**

```
Input: { force_update: boolean }

1. Check .agent/workspace-memory.md exists?
   → exists && !force_update → return CACHED
   
2. Scan file structure (fs.readdirSync recursive)
   → Build file map: path, type, purpose, size, lastModified
   → Ignore: node_modules, .git, dist, exchange/*, plan/done/*

3. Parse dependency graph (regex import/require)
   → Build adjacency list: file → [dependencies]

4. Git co-change analysis
   → git log --name-only → count co-occurrences
   → Top 10 file pairs that change together

5. Load existing KIs (if any)
   → Read ~/.gemini/antigravity/knowledge/

6. Generate .agent/workspace-memory.md
   → Template: project overview, file map, dep graph, git intel, knowledge

7. Write file + return stats
```

**Zod Schema (tool input):**

```typescript
const ScanWorkspaceInput = z.object({
  force_update: z.boolean().default(false)
    .describe('Force re-scan even if workspace-memory.md exists'),
});
```

**Tool registration:** Đăng ký trong `src/mcp-server/tools.mjs` (hoặc dedicated tools/index).

### Output format: `.agent/workspace-memory.md`

```markdown
# Workspace Memory — agent-orchestrator
> Auto-generated by scan_workspace tool
> Last updated: <ISO timestamp>

## Project Overview
- Name: agent-orchestrator
- Type: MCP server (Node.js, ESM)
- Port: 3847

## File Map
| Path | Type | Size | Purpose |
|------|------|------|---------|
| src/index.mjs | entry | 835B | Server entry point |
| ... | ... | ... | ... |

## Dependency Graph
src/mcp-server/index.mjs → config.mjs, constants.mjs, server.mjs, ...

## Git Intelligence
Top co-changing file pairs:
1. tools.mjs ↔ constants.mjs (15 times)
2. ...

## Knowledge Items
- <KI summaries if available>
```

## Files
| Action | Path |
|--------|------|
| NEW    | `src/mcp-server/tools/scan-workspace.mjs` (hoặc `.ts`) |
| MODIFY | `src/mcp-server/tools.mjs` (register tool) |

## Verification
```bash
# Start server
npm run serve  # hoặc npm run dev

# Call scan_workspace via MCP
# → Verify .agent/workspace-memory.md generated
# → File chứa đúng sections: Project Overview, File Map, Dep Graph, Git Intel
```

## Done Criteria
- [ ] `scan_workspace` tool registered và callable qua MCP
- [ ] `.agent/workspace-memory.md` được tạo đúng format
- [ ] `force_update: false` → trả về CACHED nếu file đã tồn tại
- [ ] `force_update: true` → re-scan và ghi đè
- [ ] Ignore patterns đúng (node_modules, .git, dist)
- [ ] Max 500 files scan (tránh chậm ở repo lớn)
