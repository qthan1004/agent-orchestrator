---
description: Show current task board status — pending, processing, done counts
---
# Task Status

// turbo-all

1. `node tools/task-board.mjs` → view `tasks/.tmp/board.md`
2. `node tools/check-deps.mjs` → view `tasks/.tmp/deps-check.md`
3. Tổng hợp board + tasks ready → báo user
