---
description: Display current orchestrator queue status
---
// turbo-all

# Orchestrator Status

1. Gọi tool `mcp__orchestrator__get_status()` hoặc `mcp__orchestrator__get_queue_status()` (tuỳ theo MCP tools available) để lấy dữ liệu.
2. Hiển thị bảng status cho user để nắm tiến độ work.
